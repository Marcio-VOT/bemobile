// import { faker } from '@faker-js/faker'
// import { test } from '@japa/runner'

// test.group('Client', () => {
//   const [emailU, passwordU] = [faker.internet.email(), faker.internet.password()]
//   const [ cpfC, passwordC] = [faker.number.int({max: 99_999_999_999, min: 10_000_000_000}), faker.internet.password()]
//   test('should create a new client', async ({client}) => {
//     const response = await client.post('api/user').form({ email: emailU, password: passwordU })
//     response.assertStatus(201)
//   }
// })
